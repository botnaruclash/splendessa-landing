SPLENDESSA LANDING PAGE — BLUEPRINT v4 (DESIGN-OPEN)

Status: v3 shipped with a fully locked design system. This version keeps only 
what actually matters — the story and what already works technically — and 
opens everything visual for a bold redesign pass.

🔒 LOCKED — do not change

Palette: White #FFFFFF · Charcoal #17161A · two greys max for muted text. 
Rose #B5536D stays as the brand accent — it's the color used across live 
Splendessa social assets (Facebook cover, Instagram, LinkedIn banner), so it 
can't be silently replaced. How much rose, where, in what shapes — open. 
What color it is — fixed.

Functionality:
- Final CTA → embedded Cal.com booking (15-min intro call) → Google Calendar 
  + Telegram notification.
- Secondary form → 2 fields (clinic name + phone/WhatsApp) → Google Sheets via 
  Apps Script → Telegram. Instant on-screen confirmation on submit.
- Both must keep working exactly as-is — same endpoints, same field names.

🎨 OPEN — full creative freedom

- Typography — any font(s), any pairing, no restriction to Inter.
- Header/navbar — design from scratch.
- Layout, grid, spacing, page length — no "one idea per viewport" rule anymore.
- Motion — go as bold as the direction calls for.
- Number, order, and existence of sections — merge, split, cut, reorder as the 
  new design demands.
- Interactive patterns (e.g. the before/after slider) — keep the idea if it 
  still serves the design, reinvent it if something else works better.

Still respected regardless of aesthetic direction — not style choices, don't 
cut these for "wow": prefers-reduced-motion (final states shown, no animation 
fallback), visible keyboard focus, real accessibility.

📖 STORY MATERIAL — reference, not script

Core idea: A med spa loses a client because there was no way to book at 10 PM. 
The identical clinic, with a real site, wins the same client instantly. The 
page is that comparison, twice — the failure, then the standard.

Stats available to use (keep attribution if kept):
- 75% — judge a business's credibility by its website design alone. 
  (Stanford Web Credibility Research)
- 46% — of bookings happen when the doors are closed. 
  (Phorest — 5,000+ salons & spas analyzed)

Full v3 copy for reference/reuse: see splendessa-landing-v3-FINAL.md in this repo.

Velora gate tasks (Sandu & Luca — unrelated to this redesign, still open):
- Persistent return banner on demo linking back to this page's final CTA.
- Booking calendar + instant-confirmation visible within ~10s of landing.
- Demo service naming matches whatever this page ends up calling it.
